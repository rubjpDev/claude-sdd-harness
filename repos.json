{
  "_comment": "Sample config. Replace these repos with your own. Each entry maps a logical id to a sibling repo the harness drives. Delete the frontend block if you only have one repo.",
  "repos": [
    {
      "id": "backend",
      "name": "example-backend",
      "role": "main-service",
      "path": "../example-backend",
      "working_dir": "../example-backend",
      "primary_stack": ["Python 3.12", "FastAPI", "Pydantic v2", "SQLAlchemy", "Alembic", "PostgreSQL", "Redis", "Poetry", "pytest"],
      "default_verify": ["poetry run ruff check .", "poetry run mypy app", "poetry run pytest -q"],
      "notes": "Primary backend service and main implementation surface."
    },
    {
      "id": "frontend",
      "name": "example-frontend",
      "role": "client",
      "path": "../example-frontend",
      "working_dir": "../example-frontend",
      "primary_stack": ["React", "Vite", "TypeScript", "Tailwind"],
      "default_verify": ["npm run type-check", "npm run build"],
      "notes": "Client app. Consumes the typed client generated from the backend OpenAPI."
    }
  ]
}
